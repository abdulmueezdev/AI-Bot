# Digital Clone AI Chatbot — Backend Dependencies
# Phase 1 (GSD) + Phase 2 (Ralph Loop)

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation
pydantic>=2.0
pydantic-settings>=2.0

# LLM Providers
groq>=0.9.0
httpx>=0.27.0

# Embeddings
google-genai>=2.7.0

# Vector store
chromadb>=0.5.0

# Token counting
tiktoken>=0.7.0

# Structured logging
structlog>=24.0.0

# Environment
python-dotenv>=1.0.0

# YAML parsing
pyyaml>=6.0

# ── Phase 2 — Calendar Sync ───────────────────────────────────────────
google-auth>=2.30.0
google-api-python-client>=2.140.0
cachetools>=5.4.0
APScheduler>=3.10.0

# ── Testing ───────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0

# ── Linting & Type Checking ──────────────────────────────────────────
ruff>=0.6.0
mypy>=1.11.0
